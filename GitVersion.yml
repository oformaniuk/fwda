mode: ContinuousDelivery
next-version: 0.1.1

branches:
  main:
    increment: Patch
    regex: ^main$
  feature:
    increment: Minor
    source-branches: ["main"]
  develop:
    increment: Minor

ignore:
  sha: []
