mode: ContinuousDelivery
next-version: 0.1.0

branches:
  main:
    increment: Patch
    regex: ^main$
  feature:
    increment: Minor
    source-branches: ["main"]
  develop:
    increment: Minor

ignore:
  sha: []
